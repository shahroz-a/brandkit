# React Guide

```tsx
import { OpenGraph } from "@brandkit/react";

export function Preview() {
  return (
    <OpenGraph
      tokens={{
        name: "BrandKit",
        description: "Open-source developer branding toolkit."
      }}
    />
  );
}
```

React components render SVG data URIs generated by `@brandkit/core`.
